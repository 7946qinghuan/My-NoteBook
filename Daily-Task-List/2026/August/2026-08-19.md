---
title: "2026-08-19"
date: "2026-08-19"
tags:
  - 日志
---

## 今日任务
- [ ] 提交PPT Render管线的PR代码
	- [ ] 看懂并理解整个流程


## 记录
```python
POST /task/create/slides_render
  └─ TaskService.create_slides_render()        建任务，立刻返回 task_id
       └─ 后台 runner: RenderService.run_task()
            └─ RenderService.run()             ① 组装 source.md ② 起名 ③ 调后端 ④ 传对象存储
                 └─ LocalRenderBackend.render()   并发防护 + 异常兜底
                      └─ _run()                   ★ 主流程，三段
                           ├─ 备料      建 setup 会话 → mkdir → 写 md → 初始化项目
                           ├─ _drive_skill()      建渲染会话 → run_until 驱动模型
                           └─ _collect()          找 pptx → 读回 → 读 SVG → 改名
```

待办审查：
③ 两个会话从不清理 — 待查

每次渲染建 2 个会话(setup-xxx / ppt-xxx),代码里没有任何删除。opencode 有 DELETE /session/{id}。

刚查了一下现在只有 2 个,但容器前不久重启过,历史被清空了,这个数说明不了问题。要判断得跑几次渲染再看。如果真会堆积,长期跑下去 opencode 的会话列表会越来越长(每个会话还带完整消息历史,占内存)。

这条我建议先测再改,不要凭猜加删除逻辑——万一 opencode 自己有回收,加了反而多余。

```
"models": {"Qwen/Qwen3.5-27B": {"name": "Qwen3.5-27B"}}

"models": {
        "Qwen/Qwen3.5-27B": { "name": "Qwen3.5-27B", "options": { "chat_template_kwargs": { "enable_thinking": false } } }
      }
```

一个正方形边长从 3 增加到 5，面积增加了百分之多少？只给最终答案。

## 待办 / 遗留
%% 今天没做完、要滚到明天的。复盘时会读这一段 %%
