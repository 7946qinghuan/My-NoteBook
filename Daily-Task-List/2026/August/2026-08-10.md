---
title: "2026-08-10"
date: "2026-08-10"
tags:
  - 日志
---

## 今日任务
- [ ] 回顾PPT生成管线，打通所有认知


## 记录
```bash
# 0. 停 worker（跑的是 8-06 的旧代码，无论如何要重启）
kill 1508207

# 1. 清掉三个残留调试容器 + worker 那个
docker rm -f magical_feynman boring_chandrasekhar myoc ocppt-shared

# 2. 资产（已在本地，会走校验不重下）
resources/docker/opencode/fetch-assets.sh

# 3. 重建
docker build -f resources/docker/Dockerfile.opencode -t ocppt-opencode:latest .

# 4. 核对镜像与仓库一致（这次该是 ✅）
diff <(docker run --rm --entrypoint sh ocppt-opencode:latest \
        -c 'cat ~/.config/opencode/opencode.jsonc') \
     resources/docker/opencode/opencode.jsonc \
  && echo "✅ 一致"

# 5. 起 worker，第一次渲染时容器自动重建
# 0. 停 worker（跑的是 8-06 的旧代码，无论如何要重启）
kill 1508207

# 1. 清掉三个残留调试容器 + worker 那个
docker rm -f magical_feynman boring_chandrasekhar myoc ocppt-shared

# 2. 资产（已在本地，会走校验不重下）
resources/docker/opencode/fetch-assets.sh

# 3. 重建
docker build -f resources/docker/Dockerfile.opencode -t ocppt-opencode:latest .

# 4. 核对镜像与仓库一致（这次该是 ✅）
diff <(docker run --rm --entrypoint sh ocppt-opencode:latest \
        -c 'cat ~/.config/opencode/opencode.jsonc') \
     resources/docker/opencode/opencode.jsonc \
  && echo "✅ 一致"

# 5. 起 worker，第一次渲染时容器自动重建
cd steins-algorithm && CONFIG_SOURCE=local uv run python -m steins_render.main
```

## 待办 / 遗留

