---
title: 2026-08-14
date: 2026-08-14
tags:
  - 日志
---

## 今日任务

- [x] 评审loader模块（第一轮）
	- [x] 1. `BaseLoader`只有同步`cleanup`没有异步，但是下游有两个
	- [x] 2.`batch_load`和`abatch_load`的`max_concurrency`参数没有显示指定，而是.get(参数,默认值)的方式，`下游使用时`，无法感知参数，并且不清楚为什么默认值是10
	- [x] 3. `Loader`模块中，我目前的认知是`LocalLoader`为CPU密集型，`URLLoader`为IO密集型，如果是这样，那么他们两者的`batch_load`和`abatch_load`是否应该在函数构造上有区别，那么上游的是否就不应该包含`batch_load`和`abatch_load`的通用写法
	- [x] 4. 我认为可以引入类似S3的`MinioLoader`，如果引入，如何改造`AutoLoader`
	- [x] 5. `data_type.py`文件我总感觉写的可读性差，可维护性低，是否是这样的，这个模块是否可以被优化
- [ ] 评审loader模块（第二轮）
	- [ ] 引入了`S3Loader`，他在`infrastructure.loader`模块内部，但是我们的`SourceType`和`SourceContent`在`engines.loader`模块内部，他只判断local，url，unknow，那么这是正确的吗？你之前说`S3Loader` 本质上是“外部对象存储适配器”，而不是通用加载协议的一部分：
	       - `engines/loaders`：协议、类型、路由和基础加载能力。
		- `infrastructure/loaders`：依赖特定外部系统及 SDK 的 Loader。
		- `core/bootstrap.py`：负责组装两者。
	- [ ] 

## 记录

## 待办 / 遗留

[^1]: 
