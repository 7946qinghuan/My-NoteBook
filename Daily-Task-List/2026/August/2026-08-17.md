---
title: 2026-08-17
date: 2026-08-17
tags:
  - 日志
---

## 今日任务
- [ ] 重新完善PPT生成管线
	- [ ] 1. steins-opencode服务不再启动Opencode的Docker服务，而是迁移到[新仓库](/mnt/steins/zhihao/SHOU_Project/steins-devops) ,启一个新的分支名`shou_confer_middle`,在这个分支里写opencode的Dockerfile和docker-compose,注释少写，保留最关键的就行；补充.env-template opencode的环境配置示例。构建opencode镜像， 启动opencode容器。我们后续的Opencode Docker将作为常驻中间件和MySQL，Minio等一起启动，不再单独启动。
	- [ ] 2. `.pptx`， `.svg`， `.md` 三者文件通过Opencode的file相关接口拿到文件，需要先测试该接口，接口相关文档描述在下面。
	- [ ] 3. 如果第二点验证成功，steins-opencode服务有必要再单独启动一个服务吗，是否完全可以直接在steins-agent里面编写相关代码，将相关代码迁移。
```markdown
1. ## Find files
Search for files or directories by name or pattern in the project directory.
##### query Parameters：
|   |   |
|---|---|
|directory|string|
|workspace|string|
|query<br><br>required|string|
|dirs|string<br><br>Enum: "true" "false"|
|type|string<br><br>Enum: "file" "directory"|
|limit|integer [ 1 .. 200 ]|

### Responses
200：
Array [string]
400：
|   |   |
|---|---|
|name<br><br>required|string<br><br>Value: "BadRequest"|
|data<br><br>required|object|

2.## List files
List files and directories in a specified path.
##### query Parameters
|   |   |
|---|---|
|directory|string|
|workspace|string|
|path<br><br>required|string|

### Responses
200：
Array 
|   |   |
|---|---|
|name<br><br>required|string|
|path<br><br>required|string|
|absolute<br><br>required|string|
|type<br><br>required|string<br><br>Enum: "file" "directory"|
|ignored<br><br>required|boolean|

400:
|   |   |
|---|---|
|name<br><br>required|string<br><br>Value: "BadRequest"|
|data<br><br>required|object|

3.## Read file
Read the content of a specified file.
##### query Parameters
|   |   |
|---|---|
|directory|string|
|workspace|string|
|path<br><br>required|string|


### Responses
200：
|   |   |
|---|---|
|type<br><br>required|string<br><br>Enum: "text" "binary"|
|content<br><br>required|string|
|diff|string|
|patch|object|
|encoding|string<br><br>Value: "base64"|
|mimeType|string|

400：
|   |   |
|---|---|
|name<br><br>required|string<br><br>Value: "BadRequest"|
|data<br><br>required|object|
```

了解以上需求后，我们先编写PLAN文档，保存到[路径](/mnt/steins/zhihao/SHOU_Project/shou-confer-platform/draft),我们先规划完所有需求，边界，异常等

### 缺口讨论：
1.  源文件怎么送进去?我们目前的源文件包含template文件夹和source.md文件，对吗？然后我们需要一条写入通道，也就是说我们的Docker必须使用数据卷持久化，选择Named Volume或者Bind Mount，如果Opencode容器和我们的steins-agent不再同一台虚拟服务器，可以做到文件的读写，传输操作吗？
2. API 拿不到 mtime,find_latest_pptx 的语义没法复现。我们在请求时不是绑定了工作目录吗？我们不可以先list寻找expost和饥和svg_final目录，然后再从这两个目录里面拿去文件吗？
3. 网络模式和现有 compose 互斥，这个我不太懂，需要和你讨论一下

## 记录


## 待办 / 遗留

